# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code：只保留 .claude/skills/openspeech-dev/ 作为项目约定文档，
# 其余（本地 settings、会话 lock、软链 te-industrial-frontend 等）全部不入库。
.claude/*
!.claude/skills
.claude/skills/*
!.claude/skills/openspeech-dev
!.claude/skills/openspeech-release

# Tauri signing key（若开发者把 key 放在项目内，避免误提交）
*.key

# 本地密钥 / 环境变量（Apple 签名公证密码、App-Specific Password、.p12 证书等）
.env
.env.*
!.env.example
*.p12
*.cer
*.p8

# 本地排障 / 对话总结临时文档（不入库，写正式文档去 docs/）
/test.md

# 本地临时素材（icon 源图等）
.tmp/
