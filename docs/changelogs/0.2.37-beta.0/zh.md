## v0.2.37-beta.0

### 修复
- 修复空闲一段时间后首次按下快捷键时，前 2-3 次事件被丢弃 / 后续事件突然
  burst 到前端导致状态机乱跳的问题。overlay 显示改异步调度，不再阻塞
  快捷键事件投递。

### 内部
- landing 落地页拆成 `packages/openspeech-landing` monorepo 子包，便于
  作为独立 npm lib 复用。用户侧无可见变化。

### 已知问题
- 无线麦克风功能仍未开放 UI，沿袭 0.2.36 状态，仅供内部测试。
