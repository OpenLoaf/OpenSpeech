## v0.2.56-beta.4

### 改进
- 按下快捷键后悬浮条立即显示“正在启动麦克风…”，不再等主窗口响应才出现，后台运行时反馈更快。
- 按键启动录音时不再每次先查询麦克风权限和设备列表，启动更快；启动失败时仍会提示具体原因（无权限、无设备、被占用等），并记入历史。
- 转写或 AI 整理等待较久时，悬浮条会显示已等待的秒数；AI 开始输出后显示已整理的字数，不再一直停在同一句提示上。
