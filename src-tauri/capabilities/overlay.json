{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Capabilities granted to the floating overlay window.",
  "windows": ["overlay"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:webview:default",
    "log:default",
    "store:default"
  ]
}
